#!/bin/bash -f

# libDocGen.bash, for re-building the local library documentation

# simple usage:
libDocGen.exe 

# note you can use --help to see the command arguments 
# --rootpath <dir> to generate docs from your own dirs
# --ignorefile <name> dirs to ignore
# --verbose to see what is going on
# --noconsistencycheck to not do the consistency check
# 
